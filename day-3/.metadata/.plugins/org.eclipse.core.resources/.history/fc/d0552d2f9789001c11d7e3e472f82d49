#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
 int x;
    int y;

    for(i=0;i<5;i++)
    {
                     int x=0;
                    static int y=0;
                    printf("x=%d, y=%d\n", x++,y++);
    }
    printf("\nx=%d, y=%d\n", y, x);
    system("pause");

}
